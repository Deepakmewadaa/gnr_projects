[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.11"]
build-backend = "scikit_build_core.build"

[project]
name = "customdl"
version = "0.1.0"
description = "Python bindings for CustomDL"
requires-python = ">=3.8"

[tool.scikit-build]
cmake.build-type = "Release"

